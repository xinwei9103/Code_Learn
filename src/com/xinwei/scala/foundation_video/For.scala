package com.xinwei.scala.foundation_video

/**
  * Created by xinwei on 11/16/2016.
  */
object For {

  def main(args: Array[String]): Unit = {

    /*
      flatMap 返回列表 (指针;定义;过滤器;子列表指针)
      val result = for(person <- persons; name = person.name; if !person.isMale; child <- person.children){
        yield (name,child.name) //map
      }

      val content = for(x <- List(1,2,3);y <- List(1,2,3)) yield (x,y)

     */

    val array = new Array[String](3)

  }

}
